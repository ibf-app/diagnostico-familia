<!--
  PENDENTE — o time decidiu (2026-08-04) remover o texto-rascunho anterior porque
  ainda não foi validado juridicamente. Regra de negócio confirmada e já aplicada
  no código independentemente deste texto: quando a regra de segurança (seção 4.3
  do doc de spec) é acionada, o e-mail NUNCA oferece ou menciona programa do IBF —
  mesmo que a árvore de decisão tenha atribuído um `programaRecomendado`. Isso é
  reforçado em `lib/ai-diagnostic.ts` com uma trava no código (não depende só da
  IA obedecer o prompt).

  Enquanto o texto final não é aprovado (Eduardo/Letícia + jurídico), o sistema
  usa o fallback genérico abaixo. Troque este conteúdo pelo texto oficial assim
  que aprovado — nenhuma outra mudança de código é necessária.
-->

Pelo que você compartilhou, pode ser importante conversar com um profissional
qualificado, como um psicólogo ou terapeuta familiar.
